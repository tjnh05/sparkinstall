import os
import sys
import platform

print("python version [{}]\n".format(sys.version))

python_version = platform.python_version()

if python_version != '2.7.5':
   print('''Warning: current python version {} is not 2.7.5.
         it's ignored to initialize pyspark\n'''.format(python_version))
else:
   spark_home = os.environ.get('SPARK_HOME', None)
   print("spark_home [{}]\n".format(spark_home))
   if not spark_home:
      raise ValueError('SPARK_HOME environment variable is not set')
   sys.path.insert(0, os.path.join(spark_home, 'python'))
   sys.path.insert(0, os.path.join(spark_home, 'python/lib/py4j-0.10.4-src.zip'))
   execfile(os.path.join(spark_home, 'python/pyspark/shell.py'))


git clone https://github.com/jadianes/spark-py-notebooks.git

scp kddcup.data.gz sparks0.deloittecloud.com:/opt/data/spark-py-notebooks/nb3-rdd-sampling/kddcup.data.gz

yum install python2-pip.noarch -y
pip install numpy pandas
pip install --upgrade pip
